v20.2 (v045-fix)
Updated: 2025-11-28

🔧 FIX: 로그인 페이지 리다이렉트 시 UTM 파라미터 유실 해결

NEW IN v20.2 (v045-fix):
✅ FIX: 세션 스토리지 기반 UTM 파라미터 유지 로직 추가
✅ 로그인 페이지 등 리다이렉트 시에도 UTM 파라미터 보존
✅ utm_source 빈값(-) 문제 해결
✅ 기여도 중복 계산 문제 해결 (2.9% → 0%)

HOW IT WORKS:
1. URL에 UTM 파라미터가 있으면 sessionStorage에 저장
2. URL에 UTM이 없으면 sessionStorage에서 복원
3. 로그인 페이지로 리다이렉트되어도 UTM 정보 유지
4. 브라우저 탭을 닫으면 sessionStorage 자동 삭제 (정상 동작)

DEPLOYMENT INFO:
- Backend: https://moadamda-analytics.co.kr/api/track
- Dashboard: https://dashboard.marketingzon.com
- Server: Naver Cloud Platform
- SSL: Auto-renewal (Let's Encrypt)

Previous features maintained:
- Dynamic UTM parameter collection
- Session end tracking
- DOM parsing fallback for order data
- Product tracking with multiple fallback sources
- fullyDecode() for double/multiple URL encoding
