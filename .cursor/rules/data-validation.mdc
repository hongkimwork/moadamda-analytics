---
alwaysApply: false
description: 데이터 검증 및 품질 관리 규칙 - 새 API/쿼리 개발, SQL 작성, 데이터 정제 시 필수 확인 사항
---

# 데이터 검증 및 품질 관리 규칙

**마지막 업데이트**: 2025-11-10
**목적**: 프로덕션 데이터의 정확성 보장 및 비정상 값 조기 발견

---

## 🚨 핵심 원칙

### 1. 절대 신뢰하지 말 것 (Trust Nothing, Verify Everything)
- ❌ "이 쿼리는 잘 작동할 것이다"
- ✅ "모든 데이터는 검증되어야 한다"
- ✅ "계산된 값은 합리성 체크를 거쳐야 한다"

### 2. 실제 데이터로 검증
- 새 API/쿼리 개발 시 **반드시 프로덕션 데이터로 테스트**
- 가상 시나리오만으로 개발 금지
- 사용자에게 테스트 쿼리 실행 요청

### 3. 비정상 값 감지 및 필터링
- 계산된 값이 도메인 상식에 맞는지 확인
- 임계값 설정 및 적용
- 이상 값 발견 시 즉시 보고 및 조사

---

## 📊 도메인별 임계값 (moadamda-analytics)

### 체류시간 (Time Spent)
```
✅ 정상: 1초 ~ 10분 (600초)
⚠️ 의심: 10분 ~ 1시간 (3600초)
❌ 비정상: 1시간 이상 → 필터링 필수
```

### 세션 지속시간
```
✅ 정상: 1분 ~ 30분
⚠️ 의심: 30분 ~ 2시간
❌ 비정상: 2시간 이상 (쿠키 문제 가능성)
```

### 페이지뷰 수
```
✅ 정상: 1 ~ 50 페이지
⚠️ 의심: 50 ~ 100 페이지
❌ 비정상: 100 페이지 이상 (봇 가능성)
```

### 구매 금액
```
✅ 정상: 10,000원 ~ 500,000원
⚠️ 의심: 1원 ~ 10,000원 (테스트 주문)
❌ 비정상: 0원 이하, 1,000,000원 이상
```

---

## 🔧 개발 프로세스 (필수 3단계)

### Phase 1: 데이터 탐색 (MUST DO FIRST)

**새 기능 개발 전 필수:**

```bash
# 1. 실제 데이터 샘플 확인
docker exec -i ma-postgres psql -U moadamda -d analytics -c "
  SELECT * FROM [테이블명] 
  ORDER BY timestamp DESC 
  LIMIT 5;
"

# 2. 데이터 범위 확인 (MIN/MAX/AVG)
docker exec -i ma-postgres psql -U moadamda -d analytics -c "
  SELECT 
    MIN([컬럼]), MAX([컬럼]), AVG([컬럼]),
    COUNT(*), COUNT(DISTINCT [키])
  FROM [테이블명];
"

# 3. Edge Case 확인
docker exec -i ma-postgres psql -U moadamda -d analytics -c "
  SELECT * FROM [테이블명]
  WHERE [의심 조건]
  LIMIT 10;
"
```

### Phase 2: 쿼리 작성 및 테스트

**단계별 검증:**

```sql
-- ✅ 좋은 예: WITH 절로 단계 분리
WITH step1 AS (
  SELECT ... -- 첫 번째 변환
),
step2 AS (
  SELECT ... FROM step1 -- 두 번째 변환
)
SELECT * FROM step2;

-- ❌ 나쁜 예: 한 번에 복잡하게 (검증 불가)
SELECT ... FROM (
  SELECT ... FROM (SELECT ...)
);
```

**검증 항목:**
- 계산된 값이 도메인 기준에 맞는가?
- NULL이 예상치 못한 곳에 있는가?
- JOIN으로 인한 중복이 있는가?

### Phase 3: 프론트엔드 검증 로직

**모든 계산값에 검증 추가:**

```javascript
// ✅ 올바른 예시
const totalSeconds = validPagePaths.reduce(
  (sum, p) => sum + p.time_spent_seconds, 0
);

// 합리성 체크
if (totalSeconds > 3600) {
  console.warn(`비정상적으로 긴 체류시간: ${totalSeconds}초`);
}

// 안전한 표시
const displaySeconds = Math.min(totalSeconds, 3600); // 최대 1시간
```

---

## 🚩 경고 신호 (Red Flags)

### 즉시 조사해야 할 상황

#### 1. 계산 결과가 도메인 상식에 맞지 않음
- 예: 체류시간 22일, 구매 금액 -100원
- **조치**: ⏸️ 작업 일시정지 → 데이터 원본 확인 → 계산 로직 재검토

#### 2. 같은 데이터의 다른 소스가 불일치
- 예: `order.utm_source ≠ utm_history[0].utm_source`
- **조치**: 어떤 소스가 정확한지 확인, 일관된 소스 사용

#### 3. NULL이 예상치 못한 곳에 나타남
- 예: 필수 필드가 NULL
- **조치**: 데이터 수집 로직 점검

#### 4. COUNT(*)가 기대보다 훨씬 큼
- 예: 주문 1건인데 결과 100행
- **조치**: JOIN으로 인한 중복 확인

---

## 💻 코드 패턴

### 백엔드 (검증 로직 포함)

```javascript
const query = `
  WITH validated_data AS (
    SELECT 
      *,
      -- 비정상 값 필터링
      CASE 
        WHEN time_spent > 3600 THEN NULL  -- 1시간 초과 제외
        ELSE time_spent
      END as safe_time_spent
    FROM raw_data
    WHERE time_spent > 0  -- 음수/0 제외
  )
  SELECT 
    AVG(safe_time_spent) as avg_time,
    MAX(safe_time_spent) as max_time
  FROM validated_data
  WHERE safe_time_spent IS NOT NULL;
`;

// 결과 검증
const result = await db.query(query);
if (result.rows[0].max_time > 3600) {
  console.warn('비정상 값 감지:', result.rows[0].max_time);
}
```

### 프론트엔드 (데이터 필터링)

```javascript
function OrderDetailPageContent({ orderId }) {
  const [data, setData] = useState(null);
  
  useEffect(() => {
    fetchOrderDetail().then(rawData => {
      // 데이터 검증 및 정제
      const validatedData = {
        ...rawData,
        page_path: rawData.page_path.filter(p => 
          p.time_spent_seconds > 0 && 
          p.time_spent_seconds <= 600  // 10분 제한
        )
      };
      
      setData(validatedData);
    });
  }, [orderId]);
  
  // ...
}
```

---

## 📚 실제 사례: 체류시간 버그 (2025-11-06)

### 문제
- **표시된 값**: 32,602분 = 22.6일
- **실제 값**: 수 분

### 원인
1. 백엔드 쿼리가 8일 간격의 같은 URL을 병합
2. `MIN(timestamp) = 10/29`, `MAX(timestamp) = 11/6`
3. 프론트엔드가 검증 없이 그대로 표시

### 교훈
- ❌ 합리성 체크 부재
- ❌ 실제 데이터 테스트 부재
- ❌ 도메인 지식 미적용

### 해결책
- ✅ 체류시간 임계값 설정 (10분)
- ✅ 프론트엔드 필터링 추가
- ✅ 백엔드 세션 검증 로직 추가

---

## 🎯 AI Agent 작업 규칙

### 필수 작업 일시정지 시나리오

#### 1. 새 API/쿼리 개발 시
⏸️ **작업 즉시 일시정지** → 사용자에게 테스트 쿼리 제공 → 결과 확인 → 재개

```
⏸️ 작업 일시정지 - 데이터 검증 필요

새로운 쿼리를 작성했습니다. 실제 데이터로 검증이 필요합니다.

다음 명령어를 서버에서 실행해주세요:

docker exec -i ma-postgres psql -U moadamda -d analytics -c "
  [작성한 쿼리]
"

결과를 보내주시면 검증 후 작업을 계속하겠습니다.
```

#### 2. 비정상 값 발견 시
⏸️ **작업 즉시 일시정지** → 원인 파악 쿼리 제공 → 근본 해결

#### 3. 테이블 스키마 불확실 시
⏸️ **작업 즉시 일시정지** → 스키마 확인 요청 → 재개

```bash
docker exec -i ma-postgres psql -U moadamda -d analytics -c "\d [테이블명]"
```

---

## ✅ 체크리스트 (모든 개발 시 필수)

### 개발 시작 전
- [ ] 관련 테이블 스키마 확인
- [ ] 샘플 데이터 10건 이상 확인
- [ ] 데이터 범위 (MIN/MAX/AVG) 확인

### 개발 중
- [ ] SQL 쿼리 단계별 테스트
- [ ] 중간 결과 합리성 확인
- [ ] 검증 로직 추가 (임계값, 필터링)

### 배포 전
- [ ] 실제 프로덕션 데이터로 테스트
- [ ] 여러 샘플로 검증 (정상, Edge, 비정상)
- [ ] 계산된 값이 도메인 기준에 맞는지 최종 확인

---

**핵심 원칙**: "신뢰하지 말고 검증하라 (Trust Nothing, Verify Everything)"
