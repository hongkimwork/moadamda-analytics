# Phase 4: UTM 기반 마케팅 분석 테스트 가이드

## ✅ 구현 완료 사항

### Phase 4.1: Backend - 구매 시 UTM 저장 ✅
- **파일:** `backend/src/routes/track.js`
- **변경사항:** 구매 이벤트 발생 시 visitor의 UTM 정보를 conversions 테이블에 저장
- **기능:** 어떤 광고를 보고 구매했는지 추적 가능

### Phase 4.2: Backend - UTM 성과 API 추가 ✅
- **파일:** `backend/src/routes/stats.js`
- **새 엔드포인트:** `GET /api/stats/utm-performance`
- **기능:** 매체별/캠페인별 방문자, 주문, 매출, 전환율, AOV 조회

### Phase 4.3: Frontend - 대시보드 UTM 섹션 추가 ✅
- **파일:** `frontend/src/App.jsx`
- **새 섹션:** "📢 광고 성과 (UTM 추적)"
- **기능:** 광고 성과 시각화 (테이블, 차트)

---

## 🧪 테스트 시나리오

### 시나리오 1: 인스타그램 광고 테스트 (기본)

**1단계: UTM 파라미터로 사이트 접속**

```
https://moadamda.com/?utm_source=instagram&utm_medium=ad&utm_campaign=winter_sale_test
```

**브라우저 개발자 도구 (F12) 확인:**
```
Console 탭:
[MA] Initializing Moadamda Analytics v12.0...
[MA] Pageview tracked
[MA] Event queued
```

**2단계: 상품 페이지 이동 및 구매**

1. 아무 상품이나 클릭하여 상품 상세 페이지 이동
2. 장바구니 추가
3. 주문서 작성 → 구매 완료

**브라우저 Console 확인:**
```
[MA] Product view tracked: [상품명]
[MA] Cart add tracked immediately: [상품명]
[MA] Purchase event sent immediately
```

**3단계: PostgreSQL에서 데이터 확인**

```bash
# Docker 컨테이너 접속
docker exec -it ma-postgres psql -U moadamda -d analytics

# Visitors 테이블에서 UTM 확인
SELECT visitor_id, utm_source, utm_medium, utm_campaign, first_visit 
FROM visitors 
WHERE utm_source = 'instagram' 
ORDER BY first_visit DESC 
LIMIT 5;

# Conversions 테이블에서 UTM 확인 (Phase 4.1 검증)
SELECT order_id, total_amount, final_payment, utm_source, utm_campaign, timestamp 
FROM conversions 
WHERE utm_source = 'instagram' 
ORDER BY timestamp DESC 
LIMIT 5;

# 종료
\q
```

**예상 결과:**
```
order_id     | total_amount | final_payment | utm_source  | utm_campaign
-------------|--------------|---------------|-------------|------------------
202501170001 | 50000        | 47000         | instagram   | winter_sale_test
```

**4단계: 대시보드에서 확인**

브라우저에서 대시보드 접속:
```
http://localhost:3030
```

**확인 사항:**
1. 맨 아래 "📢 광고 성과 (UTM 추적)" 섹션 표시 확인
2. 전체 통계 카드:
   - 광고 유입 방문자: 1명
   - 광고 주문: 1건
   - 광고 매출: 47,000원
   - 광고 전환율: 100%

3. "캠페인별 성과" 테이블에서 확인:
   - 매체: `instagram` (파란색 태그)
   - 캠페인: `winter_sale_test`
   - 방문자: 1
   - 주문: 1
   - 매출: 47,000원
   - 전환율: 100% (녹색)
   - AOV: 47,000원

4. "캠페인별 매출 비교" 차트 확인

---

### 시나리오 2: 여러 광고 매체 비교 테스트

**테스트 URL 목록:**

```bash
# 인스타그램 광고 1
https://moadamda.com/?utm_source=instagram&utm_medium=ad&utm_campaign=ad_creative_1

# 인스타그램 광고 2
https://moadamda.com/?utm_source=instagram&utm_medium=ad&utm_campaign=ad_creative_2

# 페이스북 광고
https://moadamda.com/?utm_source=facebook&utm_medium=cpc&utm_campaign=retargeting

# 구글 광고
https://moadamda.com/?utm_source=google&utm_medium=cpc&utm_campaign=brand_search

# 네이버 광고
https://moadamda.com/?utm_source=naver&utm_medium=cpc&utm_campaign=shopping
```

**테스트 방법:**

1. **시크릿 모드(시크릿 창)로 각 URL 접속**
   - 각 광고마다 새 시크릿 창 사용 (visitor_id 분리)
   - 상품 조회 → 장바구니 → 구매 완료

2. **5개 광고 모두 테스트 완료 후 대시보드 확인**

**예상 대시보드 화면:**

```
📢 광고 성과 (UTM 추적)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

전체 통계:
┌─────────────────┬────────┬──────────┬────────────┐
│ 광고 유입 방문자 │ 광고 주문 │ 광고 매출    │ 광고 전환율  │
├─────────────────┼────────┼──────────┼────────────┤
│ 5명             │ 5건     │ 250,000원 │ 100%      │
└─────────────────┴────────┴──────────┴────────────┘

캠페인별 성과:
┌──────────┬─────────────────┬────────┬──────┬──────────┬────────┬──────────┐
│ 매체     │ 캠페인           │ 방문자  │ 주문  │ 매출     │ 전환율  │ AOV      │
├──────────┼─────────────────┼────────┼──────┼──────────┼────────┼──────────┤
│instagram │ ad_creative_1   │ 1      │ 1    │ 50,000원 │ 100%   │ 50,000원 │
│instagram │ ad_creative_2   │ 1      │ 1    │ 50,000원 │ 100%   │ 50,000원 │
│facebook  │ retargeting     │ 1      │ 1    │ 50,000원 │ 100%   │ 50,000원 │
│google    │ brand_search    │ 1      │ 1    │ 50,000원 │ 100%   │ 50,000원 │
│naver     │ shopping        │ 1      │ 1    │ 50,000원 │ 100%   │ 50,000원 │
└──────────┴─────────────────┴────────┴──────┴──────────┴────────┴──────────┘
```

---

### 시나리오 3: 실제 광고 시뮬레이션 (GA4 문제 재현)

**목표:** GA4의 "마지막 클릭에만 100% ROAS 몰림" 문제를 실제로 시뮬레이션

**Step 1: 광고 1 클릭 (3분 체류)**

```bash
# 브라우저 시크릿 모드로 접속
https://moadamda.com/?utm_source=instagram&utm_medium=ad&utm_campaign=high_engagement

# 상품 여러 개 클릭, 3분간 탐색
# 장바구니에 담지 않고 이탈 (창 닫기)
```

**Step 2: 광고 2 클릭 (10초 체류 + 구매)**

```bash
# 같은 사용자 (시크릿 모드 아닌 일반 모드 또는 같은 시크릿 창 재오픈)
https://moadamda.com/?utm_source=instagram&utm_medium=ad&utm_campaign=quick_buy

# 빠르게 상품 클릭 → 장바구니 → 구매 완료 (10초 이내)
```

**예상 결과 (Last Click Attribution):**

대시보드에서 `quick_buy` 캠페인에만 매출 100% 귀속:

```
┌──────────┬─────────────────┬────────┬──────┬──────────┬────────┐
│ 매체     │ 캠페인           │ 방문자  │ 주문  │ 매출     │ 전환율  │
├──────────┼─────────────────┼────────┼──────┼──────────┼────────┤
│instagram │ high_engagement │ 1      │ 0    │ 0원      │ 0%     │ ← 3분 체류했지만 기여도 0
│instagram │ quick_buy       │ 1      │ 1    │ 50,000원 │ 100%   │ ← 10초만에 100% 기여도
└──────────┴─────────────────┴────────┴──────┴──────────┴────────┘
```

**이것이 GA4의 문제점!**
- 실제로는 `high_engagement` 광고가 구매 의사결정에 큰 역할
- 하지만 마지막 클릭인 `quick_buy`에만 매출 100% 귀속
- **Phase 4.4 (멀티터치 어트리뷰션)**에서 이 문제를 해결할 예정

---

## 🔍 API 직접 테스트

### PowerShell에서 API 호출

```powershell
# 오늘 기준 UTM 성과 조회
$today = Get-Date -Format "yyyy-MM-dd"
curl "http://localhost:3003/api/stats/utm-performance?start=$today&end=$today"

# 최근 1주일 UTM 성과 조회
$weekAgo = (Get-Date).AddDays(-7).ToString("yyyy-MM-dd")
$today = Get-Date -Format "yyyy-MM-dd"
curl "http://localhost:3003/api/stats/utm-performance?start=$weekAgo&end=$today"

# 모바일만 필터링
curl "http://localhost:3003/api/stats/utm-performance?start=$weekAgo&end=$today&device=mobile"
```

**예상 응답:**
```json
{
  "period": {
    "start": "2025-01-17T00:00:00.000Z",
    "end": "2025-01-17T23:59:59.999Z"
  },
  "campaigns": [
    {
      "utm_source": "instagram",
      "utm_medium": "ad",
      "utm_campaign": "winter_sale_test",
      "visitors": 1,
      "sessions": 1,
      "orders": 1,
      "revenue": 47000,
      "conversion_rate": 100,
      "aov": 47000
    }
  ],
  "total": {
    "visitors": 1,
    "sessions": 1,
    "orders": 1,
    "revenue": 47000,
    "conversion_rate": 100,
    "aov": 47000
  }
}
```

---

## 📊 디바이스 필터 테스트

### PC vs 모바일 광고 성과 비교

**1. PC에서 광고 1 클릭 → 구매**
```
PC 브라우저:
https://moadamda.com/?utm_source=google&utm_medium=cpc&utm_campaign=desktop_campaign
```

**2. 모바일에서 광고 2 클릭 → 구매**
```
모바일 브라우저:
https://moadamda.com/?utm_source=instagram&utm_medium=ad&utm_campaign=mobile_campaign
```

**3. 대시보드에서 디바이스 필터 변경**
- 디바이스 필터: **전체** → PC/모바일 광고 모두 표시
- 디바이스 필터: **PC** → google 광고만 표시
- 디바이스 필터: **모바일** → instagram 광고만 표시

---

## ⚠️ 문제 해결

### 1. "📢 광고 성과 (UTM 추적)" 섹션이 보이지 않음

**원인:** UTM 파라미터 없이 접속한 방문자만 있음

**해결:**
```bash
# UTM 파라미터 포함한 URL로 접속 후 구매 완료
https://moadamda.com/?utm_source=test&utm_medium=test&utm_campaign=test
```

### 2. 구매했는데 대시보드에 광고 주문이 0건

**원인 1:** UTM 없이 처음 접속한 visitor

**확인:**
```sql
docker exec -it ma-postgres psql -U moadamda -d analytics

SELECT visitor_id, utm_source, utm_campaign FROM visitors ORDER BY first_visit DESC LIMIT 5;
```

**원인 2:** Backend가 재시작되지 않음

**해결:**
```powershell
cd C:\analysis\moadamda-analytics
docker-compose restart backend
```

### 3. API 호출 시 500 에러

**확인:**
```powershell
# Backend 로그 확인
docker-compose logs -f backend
```

**원인:** 날짜 형식 오류

**해결:** start, end 파라미터를 `YYYY-MM-DD` 형식으로 제공
```
올바른: ?start=2025-01-17&end=2025-01-17
잘못됨: ?start=2025/01/17&end=2025/01/17
```

---

## 🎯 테스트 완료 체크리스트

- [ ] **Phase 4.1 검증:** conversions 테이블에 utm_source, utm_campaign 저장 확인
- [ ] **Phase 4.2 검증:** GET /api/stats/utm-performance API 정상 응답
- [ ] **Phase 4.3 검증:** 대시보드에 "📢 광고 성과 (UTM 추적)" 섹션 표시
- [ ] **시나리오 1:** 단일 광고 테스트 (UTM → 구매 → 대시보드)
- [ ] **시나리오 2:** 여러 매체 비교 테스트 (5개 광고)
- [ ] **시나리오 3:** GA4 문제 재현 (마지막 클릭 어트리뷰션)
- [ ] **디바이스 필터:** PC/모바일 필터링 정상 작동
- [ ] **차트 표시:** 캠페인별 매출 비교 차트 정상 표시

---

## 🚀 다음 단계: Phase 4.4 (고급 어트리뷰션)

현재는 **마지막 클릭 어트리뷰션**만 지원합니다.

**Phase 4.4에서 추가될 기능:**
- 첫 번째 클릭 어트리뷰션
- 선형 어트리뷰션 (Linear)
- 시간 감쇠 어트리뷰션 (Time Decay)
- 체류시간 기반 가중치 어트리뷰션

**예시:**
```
광고1 (3분) + 광고2 (10초) → 구매 50,000원

Phase 4.3 (현재):
  - 광고2: 50,000원 (100%)
  - 광고1: 0원 (0%)

Phase 4.4 (예정):
  - Linear: 광고1 25,000원, 광고2 25,000원
  - Engagement: 광고1 48,350원, 광고2 1,650원 (체류시간 기반)
```

---

## 📞 지원

문제가 발생하면:
1. `docker-compose logs backend` - Backend 로그 확인
2. `docker-compose logs frontend` - Frontend 로그 확인
3. 브라우저 F12 Console 확인
4. PostgreSQL 직접 확인

